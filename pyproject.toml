[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "adrd-scaffold"
version = "0.1.0"
description = "Minimal from-scratch ADRD research scaffold for discrete diffusion language modeling experiments."
requires-python = ">=3.10"

